//Напишите программу, в которую пользователь будет вводить четырёхзначный номер билета, а программа будет выводить, является ли он зеркальным, счастливым или обычным билетом.
//При решении задачи необходимо применить целочисленное деление и остаток от деления. Примеры вывода программы:

//1234 -> обычный билет 

//3425 -> счастливый билет 

//1221 -> зеркальный билет

package main

import (
	"fmt"
)

func main() {
	fmt.Print("Введите номер вашего билета: ")
  var number int 
  fmt.Scan(&number)

  isHappy := false
  isMirror := false

  if number / 1000 + (number / 100) % 10 == (number / 10) % 10 + number % 10 {
    isHappy = true
  }
  if (number / 1000 == number % 10) && ((number / 100) % 10 == (number % 100) / 10) {  
    isHappy = false
    isMirror = true
  }

  if isHappy {
    fmt.Println("Ваш билет счастливый.")
  } else if isMirror {
    fmt.Println("Ваш билет зеркальный.")
  } else {
    fmt.Println("Ваш билет обычный.")
  }
}

//Как-то очень сложно написала. Я просто пыталась придумать как сделать так, чтобы зеркальный билет не считался счастливый (потому что зеркальный не может быть не счастливым) и выводилось только сообщение про зеркальность. Придумала только так :)
